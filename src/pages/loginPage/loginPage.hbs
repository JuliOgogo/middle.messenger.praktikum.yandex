<div class="app">
    <main class="loginPage">
        <div class="loginPageWrapper">
            <div>
                <h2 class="mb40">Вход</h2>
                {{> Input name="login" id="login" type="text" placeholder="Логин"}}
                {{> Input name="password" id="password" type="password" placeholder="Пароль"}}
            </div>
            <div>
                {{> Button id="submit-login" text="Авторизоваться"}}
                {{> Link href="#" class="link" data-page="signIn" text="Нет аккаунта?"}}
            </div>
        </div>
    </main>
    {{> Footer}}
</div>
