export default `<div class="inputLabel">
  <label for="{{id}}" class="label">{{label}}</label>
  <input name="{{name}}" id="{{id}}" type="{{type}}" placeholder="{{placeholder}}" value="{{value}}" class="input">
  <p class="inputError">{{error}}</p>
</div>`
